- make all specs run without MongoDB
- implement remaining methods in Database class
- make sure associated objects are saved properly - currently only simple,
  built-in objects can be saved. It'd be nice to be able to save complex objects
  composed of other objects and retrive them
